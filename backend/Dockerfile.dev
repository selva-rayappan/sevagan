FROM node:20-alpine

WORKDIR /app

# Install dependencies in the container
COPY package*.json ./
RUN npm install --legacy-peer-deps

# Source code will be mounted via volume for hot reload
# But node_modules stays in container

EXPOSE 3000

CMD ["npm", "run", "start:dev"]
